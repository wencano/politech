{
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": ".cursor/install.sh",
  "terminals": [
    {
      "name": "dokploy compose",
      "command": "docker compose --env-file .env.dokploy -f dokploy-compose.yaml up --build"
    }
  ]
}
