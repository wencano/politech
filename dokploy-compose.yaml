# Politech — Dokploy Docker Compose (build + deploy)
#
# Dokploy default (`up -d --build`) builds politech from ./Dockerfile in this repo.
# politech uses pull_policy: build so Compose never tries to pull from Docker Hub.
#
# Optional manual two-step:
#   docker compose --env-file .env.dokploy -f dokploy-compose.yaml build politech
#   docker compose --env-file .env.dokploy -f dokploy-compose.yaml up -d
# Dokploy UI:
#   - Compose path: ./dokploy-compose.yaml
#   - Set env vars from .env.dokploy (paste into Dokploy Environment)
#   - Domains: configure in Dokploy Domains tab (port 3000 → politech service)
#
# Required env (set in Dokploy Environment):
#   DATABASE_URL        — use host `postgres` (not localhost), e.g.
#                         postgres://user:pass@postgres:5432/politech
#   APP_SECRET_KEY      — 64-char hex (openssl rand -hex 32)
#   POSTGRES_USER/PASSWORD/DB — must match credentials in DATABASE_URL

name: politech

services:
  # ── Step 1: build only (optional — same image as politech) ───────────────
  politech-build:
    profiles: [build]
    build:
      context: .
      dockerfile: Dockerfile
    image: ${POLITECH_IMAGE:-politech:latest}
    pull_policy: build
    command: ["echo", "politech image built"]

  # ── Step 2: runtime stack ────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-wencano}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-wencano}
      POSTGRES_DB: ${POSTGRES_DB:-politech}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER:-wencano} -d ${POSTGRES_DB:-politech}",
        ]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    networks:
      - politech-internal

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
      - politech-internal

  politech:
    build:
      context: .
      dockerfile: Dockerfile
    image: ${POLITECH_IMAGE:-politech:latest}
    pull_policy: build
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      APP_HOST: ${APP_HOST:-0.0.0.0}
      APP_PORT: ${APP_PORT:-3000}
      DATABASE_URL: ${DATABASE_URL:?set DATABASE_URL}
      REDIS_URL: ${REDIS_URL:-redis://redis:6379}
      APP_SECRET_KEY: ${APP_SECRET_KEY:?set APP_SECRET_KEY}
      RUST_LOG: ${RUST_LOG:-info}
    expose:
      - "${APP_PORT:-3000}"
    networks:
      - dokploy-network
      - politech-internal

volumes:
  postgres_data:

networks:
  dokploy-network:
    external: true
  politech-internal:
    driver: bridge
