// Shared UI components — reusable building blocks using Tailwind + Salesforce design tokens.
//
// Planned components:
//   badge(label, variant)       → <span class="sf-badge sf-badge-{variant}">…</span>
//   card(title, body_html)      → <section class="sf-card">…</section>
//   table(headers, rows)        → <table class="sf-table">…</table>
//   stat_card(label, value)     → stat widget for dashboard grids
//   alert(message, kind)        → info / warning / error banners
//
// Add HTML-helper functions here as pages need them.
